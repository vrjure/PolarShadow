<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="PolarShadow.Views.SearchView"
			 xmlns:ctrl="using:PolarShadow.Controls"
			 xmlns:vm="using:PolarShadow.ViewModels"
			 xmlns:polarres="using:PolarShadow.Resources"
			 x:DataType="vm:SearchViewModel"
			 x:CompileBindings="True"
			 ctrl:PageLoadAttached.RegisterLoad="True">
	<ctrl:ToolBarAttached.ToolBar>
		<!--Search box-->
		<ctrl:ToolBarTemplate ToolBar="{x:Static vm:TopLayoutViewModel.CenterTitleBarContainer}">
			<Grid ColumnDefinitions="*,2*,*">
				<ctrl:SearchBox Grid.Column="1"
								SearchText="{Binding SearchText, Mode=TwoWay}"
								SearchCommand="{Binding SearchCommand}"
								ClearCommand="{Binding ClearCommand}"/>
			</Grid>
		</ctrl:ToolBarTemplate>
	</ctrl:ToolBarAttached.ToolBar>

	<Grid Margin="10">
		<ScrollViewer>
			<StackPanel Orientation="Vertical">
				<ListBox ItemsSource="{Binding SearchResult}"
						 Background="White"
						 SelectedValue="{Binding SelectValue, Mode=TwoWay}"
						 Selection="{Binding Selection, Mode=OneWayToSource}">
					<ListBox.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel ItemWidth="{StaticResource ListBoxWrapPanelItemWidth}"/>
						</ItemsPanelTemplate>
					</ListBox.ItemsPanel>
					<ListBox.ItemTemplate>
						<DataTemplate DataType="polarres:Resource">
							<StackPanel Orientation="Vertical">
								<Image ctrl:ImageAttached.Src="{Binding ImageSrc}"
									   ctrl:ImageAttached.Cache="{Binding $parent[ListBox].((vm:SearchViewModel)DataContext).ImageCache}"/>
								<TextBlock Text="{Binding Name}"
										   TextWrapping="Wrap"/>
							</StackPanel>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
				<PathIcon Classes.spin="{Binding IsLoading}"
						  IsVisible="{Binding IsLoading}"
						  Data="{StaticResource loader2}">
				</PathIcon>
				<Button Command="{Binding LoadMoreCommand}"
						Content="&#xEF78;"
						Classes="icon"
						HorizontalAlignment="Center"
						IsVisible="{Binding ShowLoadMore}">
					
				</Button>
			</StackPanel>
		</ScrollViewer>
		<TextBlock Classes="icon"
				   Text="&#xF1F7; Empty"
				   FontSize="50"
				   HorizontalAlignment="Center"
				   VerticalAlignment="Center"
				   IsVisible="{Binding !HasData}"/>
	</Grid>
</UserControl>
